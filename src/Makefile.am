bin_PROGRAMS = cacampc
cacampc_SOURCES = main.cpp

# set the include path found by configure
INCLUDES= $(all_includes)

# the library search path.
cacampc_LDFLAGS = $(all_libraries)
noinst_HEADERS = main.h
